import React from "react";
import CircularProgress from "@mui/material/CircularProgress";
import Box from "@mui/material/Box";

export const MUIProgress = (props) => {
  return <CircularProgress {...props} />;
};
